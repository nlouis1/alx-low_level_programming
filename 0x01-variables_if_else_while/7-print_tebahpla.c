#include <stdio.h>
/**
 * main -A program to dispalay alphabet
 * Return: 0 (success)
 */
int main(void)
{
	char c = 'z';

	while
		(c >= 'a')
	{
		putchar(c);
		c--;
	}
	putchar('\n');
	return (0);
}
